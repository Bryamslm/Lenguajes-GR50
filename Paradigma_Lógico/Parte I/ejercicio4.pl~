append([],Ys,Ys).
append([X|Xs], Ys, [X|Zs]) :- append(Xs,Ys,Zs).

reverso([],[]).
reverso([X|Xs],Res):-
    reverso(Xs,XsReverso),
    append(XsReverso,[X],Res).

comparar([],[]).
comparar([A|T], [B, X]):-
    A=B,
    comparar(T, X).

invertir(L, R):-
    reverso(R, Res),
    comparar(L, Res).
